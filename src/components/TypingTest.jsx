import React, {useEffect} from 'react';

const TypingTest = () => {

    // Fetch function
    useEffect(() => null, []);

    return (
        <div className="TypingTest">
            TypingTest
        </div>
    );
};

export default TypingTest;